#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Code prints single decimal numbers from 0.
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 11; i++)
	{
		printf("%d", i);
	}
	purchar('\n');
	return (0);
}
